import { AngularHeatMapDirective } from './angular-heat-map.directive';

describe('AngularHeatMapDirective', () => {
  it('should create an instance', () => {
    const directive = new AngularHeatMapDirective();
    expect(directive).toBeTruthy();
  });
});
